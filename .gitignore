# Archivos y directorios a ignorar en Git

# Directorio de módulos de Node.js
node_modules/

# Directorio de salida de TypeScript (archivos compilados)
dist/
public/
# Archivos de log de npm (opcional, pero buena práctica)
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Archivos específicos de sistema operativo o editor (opcional, pero común)
.DS_Store # macOS
Thumbs.db # Windows
.vscode/ # Configuración de VS Code (a veces se ignora)
*.log